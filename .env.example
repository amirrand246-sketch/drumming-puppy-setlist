# Free key from https://getsongbpm.com/api — used for song tempo lookups.
# Set the same name in Netlify under Site configuration → Environment variables.
# Without it the app still runs; the tempo row just says no service is configured.
VITE_GETSONGBPM_KEY=
